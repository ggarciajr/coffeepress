extends layout

block maincontent
  .container
    div.row
      - each post in posts
        .page-header
          h1
            a(href="/post/#{post.id}")= post.title
          .content!=post.body